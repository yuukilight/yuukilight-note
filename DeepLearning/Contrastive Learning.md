对比学习不需要知道每张图的真实标签，只需要知道谁与谁相似。

Q：为什么对比学习可以是无监督学习，无监督的话如何知道谁与谁是相似的呢？

A：通过代理任务(pretext task)，即一种规则，来定义谁与谁相似。 

对比学习的范式就是：代理任务 + 目标函数

例如: 假设有样本 $x_i$(i = 0, 1, 2, 3 ...), 对其进行数据增强处理得到 $x_{i_m}$ 和 $x_{i_n}$,将$x_{i_m}$和$x_{j_n}$作为对比的对象。当$i = j$ 时互为正样本，当$i \neq j$时互为负样本。

